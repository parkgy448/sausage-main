<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>관리자 페이지 | SSS CASINO</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --color-bg: #020617;
            --color-bg-light: #0f172a;
            --color-bg-lighter: #1e293b;
            --color-border: #334155;
            --color-text: #f9fafb;
            --color-text-dim: #9ca3af;
            --color-primary: #f97316;
            --color-accent: #6366f1;
            --color-approve: #10b981;
            --color-reject: #ef4444;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background-color: var(--color-bg);
            color: var(--color-text);
        }
        .admin-tab.active {
            background-color: var(--color-primary);
            color: var(--color-text);
            border-bottom-color: var(--color-primary);
        }
        .admin-tab {
            border-bottom: 2px solid transparent;
        }
        .tab-content { display: none; }
        .tab-content.active { display: block; }
        .stat-card {
            background: linear-gradient(145deg, var(--color-bg-light), var(--color-bg));
            border: 1px solid var(--color-border);
        }
        .admin-action-btn {
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 11px;
            margin: 2px;
            border: none;
            cursor: pointer;
            transition: all 0.2s ease;
            background-color: var(--color-border);
        }
        .admin-action-btn:hover { filter: brightness(1.2); }
        .admin-action-btn.info { background-color: var(--color-accent); }
        .admin-action-btn.approve { background-color: var(--color-approve); }
        .admin-action-btn.reject { background-color: var(--color-reject); }
        
        #userTableBody input, #userTableBody select, .styled-input, .styled-textarea {
             background-color: var(--color-bg-light);
             padding: 6px 8px;
             border-radius: 6px;
             border: 1px solid var(--color-border);
             font-size: 13px;
        }
        #userTableBody input:focus, #userTableBody select:focus, .styled-input:focus, .styled-textarea:focus {
            outline: none;
            border-color: var(--color-primary);
            box-shadow: 0 0 0 2px rgba(249, 115, 22, 0.5);
        }
        .primary-btn {
            background: linear-gradient(90deg, var(--color-accent), #ec4899);
            color: var(--color-text);
            font-weight: 600;
            cursor: pointer;
            padding: 10px;
            border-radius: 999px;
            border: none;
        }
        .overlay-screen {
            position: fixed;
            inset: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(2, 6, 23, 0.9);
            backdrop-filter: blur(4px);
            z-index: 100;
        }
    </style>
</head>
<body class="min-h-screen">

    <div id="adminPanel" class="container mx-auto px-4 py-6 hidden">
        <!-- Header -->
        <header class="flex justify-between items-center mb-6">
            <div>
                <h1 class="text-2xl font-bold text-orange-400">SSS CASINO Admin</h1>
                <div id="adminEmail" class="text-sm text-gray-400"></div>
            </div>
            <a href="index.html" class="text-gray-400 hover:text-white border border-gray-600 rounded-full px-4 py-2 text-sm">메인으로</a>
        </header>

        <!-- 탭 메뉴 -->
        <div class="flex overflow-x-auto border-b border-gray-700 mb-6">
            <button class="admin-tab active px-5 py-3 font-bold whitespace-nowrap" data-tab="users">회원 관리</button>
            <button class="admin-tab px-5 py-3 font-bold whitespace-nowrap" data-tab="approvals">가입 승인</button>
            <button class="admin-tab px-5 py-3 font-bold whitespace-nowrap" data-tab="messages">쪽지 관리</button>
            <button class="admin-tab px-5 py-3 font-bold whitespace-nowrap" data-tab="deposits">충전 관리</button>
            <button class="admin-tab px-5 py-3 font-bold whitespace-nowrap" data-tab="exchanges">환전 관리</button>
            <button class="admin-tab px-5 py-3 font-bold whitespace-nowrap" data-tab="chats">고객 문의</button>
        </div>

        <!-- Users Tab -->
        <div id="usersTab" class="tab-content active">
            <div class="mb-4">
                <input type="text" id="userSearch" placeholder="닉네임 또는 이메일로 검색..." class="w-full max-w-sm styled-input">
            </div>
            <div class="bg-slate-800/50 rounded-lg overflow-hidden border border-slate-700">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-slate-700/50">
                            <tr>
                                <th class="p-3 text-left text-sm font-semibold">닉네임/이메일</th>
                                <th class="p-3 text-left text-sm font-semibold">보유머니</th>
                                <th class="p-3 text-left text-sm font-semibold">포인트</th>
                                <th class="p-3 text-left text-sm font-semibold">가입일</th>
                                <th class="p-3 text-left text-sm font-semibold">역할/상태</th>
                                <th class="p-3 text-left text-sm font-semibold">관리</th>
                            </tr>
                        </thead>
                        <tbody id="userTableBody" class="divide-y divide-slate-700"></tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Approvals Tab -->
        <div id="approvalsTab" class="tab-content">
             <div class="bg-slate-800/50 rounded-lg overflow-hidden border border-slate-700">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-slate-700/50">
                            <tr>
                                <th class="p-3 text-left text-sm font-semibold">신청자 (닉네임/이메일)</th>
                                <th class="p-3 text-left text-sm font-semibold">가입일</th>
                                <th class="p-3 text-left text-sm font-semibold">관리</th>
                            </tr>
                        </thead>
                        <tbody id="approvalsTableBody" class="divide-y divide-slate-700"></tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Messages Tab -->
        <div id="messagesTab" class="tab-content">
             <div class="bg-slate-800/50 rounded-lg p-6 border border-slate-700 max-w-2xl mx-auto">
                <h3 class="font-bold mb-4 text-orange-400 text-lg">전체 사용자에게 쪽지 보내기</h3>
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm text-gray-400 mb-1">쪽지 제목</label>
                        <input type="text" id="messageTitle" class="w-full styled-input" placeholder="중요 공지사항">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-400 mb-1">쪽지 내용</label>
                        <textarea id="messageContent" class="w-full styled-textarea" rows="5" placeholder="내용을 입력하세요..."></textarea>
                    </div>
                    <button id="sendMessageBtn" class="w-full primary-btn !rounded-md !py-2.5">
                        전체 발송
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Deposits Tab -->
        <div id="depositsTab" class="tab-content">
             <div class="bg-slate-800/50 rounded-lg overflow-hidden border border-slate-700">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-slate-700/50">
                            <tr>
                                <th class="p-3 text-left text-sm font-semibold">신청자 (닉네임)</th>
                                <th class="p-3 text-left text-sm font-semibold">신청금액</th>
                                <th class="p-3 text-left text-sm font-semibold">신청일</th>
                                <th class="p-3 text-left text-sm font-semibold">관리</th>
                            </tr>
                        </thead>
                        <tbody id="depositTableBody" class="divide-y divide-slate-700"></tbody>
                    </table>
                </div>
            </div>
        </div>
        
        <!-- Exchanges Tab -->
        <div id="exchangesTab" class="tab-content">
             <div class="bg-slate-800/50 rounded-lg overflow-hidden border border-slate-700">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead class="bg-slate-700/50">
                            <tr>
                                <th class="p-3 text-left text-sm font-semibold">신청자 (닉네임)</th>
                                <th class="p-3 text-left text-sm font-semibold">신청금액</th>
                                <th class="p-3 text-left text-sm font-semibold">은행/계좌/예금주</th>
                                <th class="p-3 text-left text-sm font-semibold">신청일</th>
                                <th class="p-3 text-left text-sm font-semibold">관리</th>
                            </tr>
                        </thead>
                        <tbody id="exchangeTableBody" class="divide-y divide-slate-700"></tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Chats Tab -->
        <div id="chatsTab" class="tab-content">
            <div class="flex h-[60vh] bg-slate-800/50 rounded-lg border border-slate-700">
                <!-- Left Panel: Chat List -->
                <div class="w-1/3 border-r border-slate-700 flex flex-col">
                    <div class="p-3 border-b border-slate-700 font-bold">채팅 목록</div>
                    <div id="chatListContainer" class="overflow-y-auto flex-1">
                        <!-- Chat list items will be injected here -->
                    </div>
                </div>
                <!-- Right Panel: Chat Window -->
                <div id="chatWindowContainer" class="w-2/3 flex-col hidden">
                    <!-- Chat window will be injected here -->
                </div>
            </div>
        </div>
    </div>
    
    <div id="accessDenied" class="overlay-screen">
        <div class="text-center p-4">
            <h2 class="text-2xl font-bold text-red-500">액세스 거부됨</h2>
            <p class="text-gray-400 mt-2">관리자만 이 페이지에 접근할 수 있습니다. 5초 후 메인 페이지로 이동합니다.</p>
        </div>
    </div>

    <div id="loadingIndicator" class="overlay-screen hidden">
         <div class="text-center">
            <svg class="animate-spin h-8 w-8 text-orange-500 mx-auto" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
            <h2 class="text-xl font-bold text-orange-500 mt-4">처리 중...</h2>
        </div>
    </div>

    <script type="module">
        import { auth, db } from "./js/firebase-config.js";
        import { listenAuth, fetchCurrentUserProfile } from "./js/auth-utils.js";
        import { collection, getDocs, doc, updateDoc, deleteDoc, writeBatch, serverTimestamp, query, where, orderBy, runTransaction, onSnapshot, addDoc, increment } from "https://www.gstatic.com/firebasejs/10.13.0/firebase-firestore.js";

        // --- DOM Elements ---
        const adminPanel = document.getElementById('adminPanel');
        const accessDenied = document.getElementById('accessDenied');
        const loadingIndicator = document.getElementById('loadingIndicator');
        const adminEmailEl = document.getElementById('adminEmail');
        
        // --- Caches ---
        let allUsersCache = new Map();
        let pendingUsersCache = [];
        let depositRequestsCache = [];
        let exchangeRequestsCache = [];
        
        // --- Real-time listeners ---
        let activeChatsUnsubscribe = null;
        let chatMessagesUnsubscribe = null;

        // --- Initialization & Auth ---
        listenAuth(async (user) => {
            if (user) {
                try {
                    const profile = await fetchCurrentUserProfile(user.uid);
                    if (profile && profile.role === 'admin') {
                        initializeAdminPanel(user, profile);
                    } else {
                        showAccessDenied();
                    }
                } catch (e) {
                    showAccessDenied();
                }
            } else {
                showAccessDenied();
            }
        });

        function showAccessDenied() {
            adminPanel.classList.add('hidden');
            accessDenied.classList.remove('hidden');
            setTimeout(() => { window.location.href = 'index.html'; }, 3000);
        }

        async function initializeAdminPanel(user, profile) {
            loadingIndicator.classList.remove('hidden');
            adminEmailEl.textContent = user.email;
            
            await loadAllUsers();
            
            setupTabs();
            setupUserManagement();
            setupApprovalManagement();
            setupMessageManagement();
            setupDepositManagement();
            setupExchangeManagement();
            setupChatManagement();
            
            document.querySelector('.admin-tab[data-tab="users"]').click();
            
            adminPanel.classList.remove('hidden');
            loadingIndicator.classList.add('hidden');
        }

        async function loadAllUsers() {
            try {
                const usersSnapshot = await getDocs(collection(db, "users"));
                allUsersCache.clear();
                usersSnapshot.docs.forEach(doc => allUsersCache.set(doc.id, { id: doc.id, ...doc.data() }));
            } catch (error) {
                console.error("Error loading users:", error);
                alert('사용자 목록을 불러오는 데 실패했습니다. Firestore 보안 규칙을 확인하세요.');
            }
        }
        
        function setupTabs() {
            document.querySelectorAll('.admin-tab').forEach(tab => {
                tab.addEventListener('click', async function() {
                    if (this.disabled) return;
                    
                    if (activeChatsUnsubscribe) { activeChatsUnsubscribe(); activeChatsUnsubscribe = null; }
                    if (chatMessagesUnsubscribe) { chatMessagesUnsubscribe(); chatMessagesUnsubscribe = null; }

                    document.querySelectorAll('.admin-tab').forEach(t => t.classList.remove('active'));
                    document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));

                    this.classList.add('active');
                    const tabName = this.dataset.tab;
                    document.getElementById(tabName + 'Tab').classList.add('active');
                    
                    switch (tabName) {
                        case 'users': renderUserTable(); break;
                        case 'approvals': await loadPendingUsers(); renderApprovalsTable(); break;
                        case 'deposits': await loadDepositRequests(); renderDepositTable(); break;
                        case 'exchanges': await loadExchangeRequests(); renderExchangeTable(); break;
                        case 'chats': loadActiveChats(); break;
                    }
                });
            });
        }
        
        // --- Feature Setups ---
        function setupUserManagement() {
            document.getElementById('userSearch').addEventListener('input', () => renderUserTable());
            const userTableBody = document.getElementById('userTableBody');
            
            userTableBody.addEventListener('change', async (e) => {
                const target = e.target;
                const id = target.dataset.id;
                if (!id || !target.dataset.field) return;

                const field = target.dataset.field;
                let value = target.type === 'number' ? Number(target.value) : target.value;
                if (target.type === 'number' && (isNaN(value) || value < 0)) {
                    alert('유효한 숫자를 입력하세요.');
                    target.value = allUsersCache.get(id)[field];
                    return;
                }
                
                loadingIndicator.classList.remove('hidden');
                try {
                    await updateDoc(doc(db, "users", id), { [field]: value });
                    allUsersCache.get(id)[field] = value;
                } catch (error) { alert('업데이트 실패'); } 
                finally { loadingIndicator.classList.add('hidden'); }
            });

            userTableBody.addEventListener('click', async (e) => {
                if (e.target.classList.contains('delete')) {
                    const id = e.target.dataset.id;
                    if (confirm(`'${allUsersCache.get(id)?.nickname}' 사용자를 삭제하시겠습니까?`)) {
                        loadingIndicator.classList.remove('hidden');
                        try {
                            await deleteDoc(doc(db, "users", id));
                            allUsersCache.delete(id);
                            renderUserTable();
                            alert('사용자가 삭제되었습니다.');
                        } catch (error) { alert('사용자 삭제 실패'); } 
                        finally { loadingIndicator.classList.add('hidden'); }
                    }
                }
            });
        }

        function renderUserTable() {
            const userTableBody = document.getElementById('userTableBody');
            const searchTerm = document.getElementById('userSearch').value.toLowerCase();
            const usersToRender = Array.from(allUsersCache.values()).filter(user => (user.nickname?.toLowerCase() || '').includes(searchTerm) || (user.email?.toLowerCase() || '').includes(searchTerm));
            
            userTableBody.innerHTML = usersToRender.length === 0 ? `<tr><td colspan="6" class="text-center p-5 text-gray-500">표시할 사용자가 없습니다.</td></tr>` : usersToRender.map(user => `
                <tr>
                    <td class="p-3"><div class="font-medium">${user.nickname || 'N/A'}</div><div class="text-xs text-gray-400">${user.email}</div></td>
                    <td class="p-3"><input type="number" value="${user.money || 0}" data-id="${user.id}" data-field="money" class="w-28"> 원</td>
                    <td class="p-3"><input type="number" value="${user.point || 0}" data-id="${user.id}" data-field="point" class="w-24"> P</td>
                    <td class="p-3 text-sm">${user.createdAt?.toDate?.().toLocaleDateString() || '알 수 없음'}</td>
                    <td class="p-3">
                        <select data-id="${user.id}" data-field="role" class="w-24 mb-1"><option value="user" ${user.role === 'user' ? 'selected' : ''}>User</option><option value="admin" ${user.role === 'admin' ? 'selected' : ''}>Admin</option></select>
                        <select data-id="${user.id}" data-field="status" class="w-24"><option value="active" ${user.status === 'active' ? 'selected' : ''}>Active</option><option value="pending" ${user.status === 'pending' ? 'selected' : ''}>Pending</option></select>
                    </td>
                    <td class="p-3"><button class="admin-action-btn reject delete" data-id="${user.id}">삭제</button></td>
                </tr>
            `).join('');
        }

        async function loadPendingUsers() { /* ... */ }
        function renderApprovalsTable() { /* ... */ }
        function setupApprovalManagement() { /* ... */ }
        function setupMessageManagement() { /* ... */ }
        async function loadDepositRequests() { /* ... */ }
        function renderDepositTable() { /* ... */ }
        function setupDepositManagement() { /* ... */ }
        async function loadExchangeRequests() { /* ... */ }
        function renderExchangeTable() { /* ... */ }
        function setupExchangeManagement() { /* ... */ }

        // --- Chat Functions ---
        function setupChatManagement() {
            document.getElementById('chatListContainer').addEventListener('click', (e) => {
                const chatItem = e.target.closest('.chat-item');
                if (chatItem) {
                    const { userId, userName } = chatItem.dataset;
                    openChatWindow(userId, userName);
                }
            });
        }
        
        function loadActiveChats() {
            const q = query(collection(db, "supportChats"), orderBy("updatedAt", "desc"));
            if (activeChatsUnsubscribe) activeChatsUnsubscribe();
            
            activeChatsUnsubscribe = onSnapshot(q, (snapshot) => {
                const chatListContainer = document.getElementById('chatListContainer');
                chatListContainer.innerHTML = snapshot.empty ? `<p class="p-4 text-center text-gray-500">활성 채팅이 없습니다.</p>` : snapshot.docs.map(doc => {
                    const chat = doc.data();
                    return `
                        <div class="chat-item p-3 border-b border-slate-700 cursor-pointer hover:bg-slate-700/50" data-user-id="${chat.userId}" data-user-name="${chat.userName}">
                            <div class="flex justify-between items-center">
                                <span class="font-bold">${chat.userName}</span>
                                ${chat.unreadByAdmin > 0 ? `<span class="bg-red-500 text-white text-xs rounded-full px-2 py-1">${chat.unreadByAdmin}</span>` : ''}
                            </div>
                            <p class="text-sm text-gray-400 truncate">${chat.lastMessage || '...'}</p>
                        </div>
                    `;
                }).join('');
            });
        }
        
        function openChatWindow(userId, userName) {
            const chatWindowContainer = document.getElementById('chatWindowContainer');
            chatWindowContainer.classList.remove('hidden');
            chatWindowContainer.style.display = 'flex';
            chatWindowContainer.innerHTML = `
                <div class="p-3 border-b border-slate-700 font-bold flex items-center justify-between"><span>${userName}</span></div>
                <div id="messagesContainer" class="flex-1 p-4 overflow-y-auto"></div>
                <div class="p-2 border-t border-slate-700">
                    <textarea id="chatInput" class="w-full styled-textarea" rows="2" placeholder="메시지를 입력하세요..."></textarea>
                    <button id="sendChatBtn" class="w-full primary-btn !rounded-md !py-2 mt-2">전송</button>
                </div>
            `;
            
            document.getElementById('sendChatBtn').onclick = () => sendAdminMessage(userId);
            document.getElementById('chatInput').onkeypress = (e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendAdminMessage(userId); } };
            
            if (chatMessagesUnsubscribe) chatMessagesUnsubscribe();
            const messagesQuery = query(collection(db, "supportChats", userId, "messages"), orderBy("timestamp", "asc"));
            chatMessagesUnsubscribe = onSnapshot(messagesQuery, (snapshot) => {
                const messagesContainer = document.getElementById('messagesContainer');
                messagesContainer.innerHTML = snapshot.docs.map(doc => {
                    const msg = doc.data();
                    return `
                        <div class="flex mb-2 ${msg.sender === 'admin' ? 'justify-end' : 'justify-start'}">
                            <div class="p-2 rounded-lg max-w-xs ${msg.sender === 'admin' ? 'bg-indigo-500' : 'bg-slate-600'}">
                                ${msg.text.replace(/\n/g, '<br>')}
                                <div class="text-xs text-right opacity-70 mt-1">${msg.timestamp?.toDate().toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) || ''}</div>
                            </div>
                        </div>
                    `;
                }).join('');
                messagesContainer.scrollTop = messagesContainer.scrollHeight;
            });
            
            updateDoc(doc(db, "supportChats", userId), { unreadByAdmin: 0 });
        }
        
        async function sendAdminMessage(userId) {
            const input = document.getElementById('chatInput');
            const text = input.value.trim();
            if (!text) return;
            input.value = '';

            await addDoc(collection(db, "supportChats", userId, "messages"), { text, sender: 'admin', timestamp: serverTimestamp() });
            await updateDoc(doc(db, "supportChats", userId), { lastMessage: text, updatedAt: serverTimestamp(), unreadByUser: increment(1) });
        }

    </script>
</body>
</html>

</body>
</html>